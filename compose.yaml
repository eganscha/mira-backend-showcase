services:
  backend:
    build:
      context: .
      dockerfile: containers/backend/Dockerfile
      target: dev
    init: true
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app